// Integers.
N_RUNS=125 // Number of runs.
WIDTH=[1,2,5,10] // Width of the array (i.e. the size of the new dimension).
LENGTH=[100,50,20,10] // World's length (lambda in previous models).
START_LENGTH=[50,25,10,5] // Initial border between Ns and Ms (n in previous models).

// Doubles.
WN=0.5 // Relative fitness of Neanderthals (fitness of Moderns is 1 - this).

// Strings.
WRITE_PATH=data // The directory to which data is written.