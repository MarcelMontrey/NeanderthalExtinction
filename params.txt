// Integers.
N_RUNS=125 // Number of runs.
WIDTH=[1,2,5,10] // Width of the array (i.e. the size of the new dimension).
DEPTH=[100,50,20,10] // World's depth (lambda in previous models).
START_DEPTH=[50,25,10,5] // Initial border between Ns and Ms (n in previous models).

// Doubles.
DEATH_RATE=0.01
WM=0.5 // Relative fitness of Moderns (fitness of Neanderthals is 1 - this).

// Strings.
WRITE_PATH=data // The directory to which data is written.